<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/gameBaseContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#00FFFFFF"
    android:backgroundTint="#00FFFFFF"
    android:foregroundTint="#00FFFFFF">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/timeFragmentContainer"
        android:name="net.wackwack.pic_card_memory.game.view.ElapsedTimeFragment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout="@layout/fragment_elapsed_time" />

    <ImageButton
        android:id="@+id/musicOnToOffButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:background="@null"
        android:contentDescription="@string/text_volume_button_description"
        android:src="@drawable/baseline_music_note_24"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/musicOffToOnButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:background="@null"
        android:contentDescription="@string/text_mute_button_description"
        android:src="@drawable/baseline_music_off_24"
        android:visibility="invisible"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <FrameLayout
        android:id="@+id/gameMainContainer"
        android:layout_width="match_parent"
        android:layout_height="712dp"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@+id/musicOnToOffButton"
        tools:layout_editor_absoluteX="-32dp">

    </FrameLayout>
</androidx.constraintlayout.widget.ConstraintLayout>